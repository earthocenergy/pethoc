"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const{error}=await createClient().auth.signInWithPassword({email,password});if(error)return setMsg(error.message);location.href="/profile"}return <main className="shell"><div className="brand">PET<span>HOC</span></div><section className="card"><h1>Welcome back</h1><form onSubmit={submit}><p><input style={{width:"100%",padding:14}} placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/></p><p><input style={{width:"100%",padding:14}} type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/></p><button className="btn">Sign in</button></form>{msg&&<p>{msg}</p>}</section></main>}