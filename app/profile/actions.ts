"use server";import{createClient}from"@/lib/supabase/server";import{redirect}from"next/navigation";export async function saveProfile(fd:FormData){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/auth");await s.from("profiles").update({display_name:String(fd.get("name")||""),phone:String(fd.get("phone")||""),default_address:String(fd.get("address")||"")}).eq("id",user.id);redirect("/profile")}export async function logout(){const s=await createClient();await s.auth.signOut();redirect("/auth")}