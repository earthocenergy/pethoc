import {createClient} from "@/lib/supabase/server";import Link from "next/link";export default async function Orders(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)return <main className="shell"><h1>Orders</h1><Link className="btn" href="/login">Sign in</Link></main>;const{data}=await s.from("orders").select("*").eq("customer_user_id",user.id).order("created_at",{ascending:false});return <main className="shell"><h1>Your orders</h1>{data?.length?data.map(o=><section className="card" key={o.id}><b>{o.order_code}</b><p>{o.quantity_kg} kg · {o.status.replaceAll("_"," ")}</p></section>):<section className="card">No orders yet.</section>}<Link className="btn" href="/">Back home</Link></main>}