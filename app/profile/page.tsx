import {createClient} from "@/lib/supabase/server";import Link from "next/link";export default async function Profile(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)return <main className="shell"><h1>Profile</h1><p>Sign in to access your Pethoc profile.</p><Link className="btn" href="/login">Sign in</Link></main>;const{data:p}=await s.from("profiles").select("*").eq("id",user.id).maybeSingle();return <main className="shell"><h1>Profile</h1><section className="card"><b>{p?.display_name||user.email}</b><p>{user.email}</p><p className="muted">{p?.default_address||"No delivery address saved yet"}</p><p>Role: {p?.role||"customer"}</p></section><Link href="/" className="btn">Back home</Link></main>}