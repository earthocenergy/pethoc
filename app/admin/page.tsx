import {createClient} from "@/lib/supabase/server";export default async function Admin(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)return <main className="shell"><h1>Admin</h1><p>Please sign in.</p></main>;const{data:p}=await s.from("profiles").select("role").eq("id",user.id).maybeSingle();if(p?.role!=="admin")return <main className="shell"><h1>Access denied</h1></main>;const[{count:orders},{count:vendors},{count:customers}]=await Promise.all([s.from("orders").select("*",{count:"exact",head:true}),s.from("vendor_profiles").select("*",{count:"exact",head:true}),s.from("profiles").select("*",{count:"exact",head:true}).eq("role","customer")]);return <main className="shell"><h1>Operations overview</h1><div className="grid"><div className="card"><b>{orders||0}</b><br/>Orders</div><div className="card"><b>{vendors||0}</b><br/>Vendors</div><div className="card"><b>{customers||0}</b><br/>Customers</div></div></main>}