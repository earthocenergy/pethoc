import{createClient}from"@/lib/supabase/server";import{saveProfile,logout}from"./actions";import Link from"next/link";export default async function Profile(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)return <main className="shell"><Link className="btn" href="/auth">Sign in</Link></main>;const{data:p}=await s.from("profiles").select("*").eq("id",user.id).maybeSingle();return <main className="shell"><h1>Profile</h1><form action={saveProfile} className="card"><label>Name</label><input className="field" name="name" defaultValue={p?.display_name||""}/><label>Phone</label><input className="field" name="phone" defaultValue={p?.phone||""}/><label>Delivery address</label><textarea className="field" name="address" defaultValue={p?.default_address||""}/><button className="btn">Save profile</button></form><p className="muted">Role: {p?.role||"customer"}</p><form action={logout}><button className="btn danger">Sign out</button></form></main>}